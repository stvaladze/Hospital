<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.solvd.hospital.mybatis.mapper.DoctorMapper">

    <resultMap id="DoctorResultMap" type="com.solvd.hospital.model.Doctor">
        <id     property="id"        column="id"/>
        <result property="firstName" column="first_name"/>
        <result property="lastName"  column="last_name"/>
    </resultMap>

    <insert id="insert" parameterType="com.solvd.hospital.model.Doctor">
        INSERT INTO doctors(first_name, last_name)
        VALUES (#{firstName}, #{lastName})
    </insert>

    <select id="selectById" parameterType="int" resultMap="DoctorResultMap">
        SELECT * FROM doctors WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="DoctorResultMap">
        SELECT * FROM doctors
    </select>

    <update id="update" parameterType="com.solvd.hospital.model.Doctor">
        UPDATE doctors
        SET first_name = #{firstName}, last_name = #{lastName}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM doctors WHERE id = #{id}
    </delete>

</mapper>